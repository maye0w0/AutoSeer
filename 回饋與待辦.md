# AutoSeer 回饋與待辦

這是 AutoSeer 的**建議／回饋／BUG 追蹤清單**。新的回饋依下方格式往對應區塊新增，並隨處理進度更新狀態。

## 狀態圖例
🔴 未開始　🟡 進行中　🟢 已完成　🔵 長期／架構　❓ 待釐清

## 新增項目格式
```
- **編號｜標題**（來源：<出處>）<狀態emoji> <優先>
  - 描述：使用者回報的內容
  - 判斷：技術定位（可能的話附「否證條件」）
  - 做法：建議實作方向
  - 關聯：相關程式檔
```

## 目前優先序（使用者指定 FR-2 延遲 > FR-1 循環）
1. **FR-2 可調延遲（含撤退精準點擊）** — 使用者指定最優先：點太快沒真正按到、誤開選單→辨識失效，是當前最痛點
2. BUG-1 重複授權多浮窗、FR-1 精靈因子循環（兩者直接壞掉當前使用）
3. FR-4 單一匯出、UX-1 文案（高效益、低風險）
4. FR-3 編輯器滑動重排
5. FR-5 / FR-6 / FR-7（架構性、中長期，部分需遊戲截圖）

---

## A. 已完成（本階段，供對照）
- 🟢 視覺化編輯器保留 `*N`（狂點至陣亡）往返修正
- 🟢 多腳本管理（儲存／切換／匯入匯出／技能格顯示名稱）
- 🟢 GitHub Actions CI（雲端建 debug APK，Actions → Artifacts 下載）
- 🟢 階段2啟動器：`DeviceTemplates` 裝置端樣板載入、懸浮列「存畫面」「測試偵測」

---

## B. BUG

- **BUG-1｜重複按「授權螢幕擷取並啟動」→ 疊出多個浮動視窗**（來源：建議_2 BUG#1）🟢 已完成（2026-09-18）
  - 判斷：`AutoSeerService.setup()` 每次授權都新建 `ControlOverlay`（及 `capture`/`runner`）卻沒移除舊的，舊資源外洩、浮窗疊加。
  - 實作：抽出 `releaseRuntime()`（釋放 overlay/capture/runner，不停服務），`setup()` 開頭先呼叫它再以新的投影重建；`stopEverything()` 亦復用之。重複授權不再疊窗。
  - 關聯：`app/src/main/java/com/autoseer/service/AutoSeerService.kt`。

---

## C. 功能建議

- **FR-1｜精靈因子固定關卡循環**（來源：建議_2 #5 #6 #8，三條同源）🟢 已完成（2026-09-18）
  - 現象：精靈因子＝5 關，清完第5關遊戲自動回第1關（每日 3 輪）。舊版 `stageIndex` 無上限遞增，清完第5關顯示「關卡6」、用「預設技能格2」狂點、`maxBattles=30` 又不停 →6→7→8…（#5/#8）。
  - 實作：`BattlePlan` 加 `loops`（精靈因子=3），並提供 `stagesPerLoop`(=stages.size)、`stageIndexFor(cleared)=(startStage-1+cleared) % stagesPerLoop` 環繞、`clearsTarget()=stagesPerLoop*loops-(startStage-1)`。`BattleScript` 用環繞索引取關卡、`reachedLimit()` 用 `clearsTarget()`（與 `maxBattles` 取先到者）。#8：固定關卡數不再跑出不存在的關。#6：中途 `startStage=3` 接續時，首個部分輪算消耗一次，`clearsTarget` 自動補足剩餘整輪。空計畫不環繞、只受 `maxBattles` 限。
  - UI：腳本編輯頁新增「循環輪數」；`SeerScript.loops` 持久化（JSON）。
  - 測試：`loopWrapsWithinFixedStageCount`、`loopStartStageMidLoopConsumesOneAttempt`、`emptyPlanHasNoLoopLimitNorWrap`。
  - 關聯：`scripts/.../BattlePlan.kt`、`BattleScript.kt`、`BattlePlanParser.kt`、`app/.../core/SeerScript.kt`、`ScriptStore.kt`、`ui/ScriptEditActivity.kt`。

- **FR-2｜可調延遲（含撤退精準點擊）**（來源：建議_2 #2 #9）🟢 已完成（2026-09-18）
  - 實作：分段延遲、全域共用。主頁「延遲設定（全域）」→ 6 段可調 ms（恢復／進戰／技能／換精靈／結算／撤退），預設 500、可還原。撤退改「延遲＋精準單點」序列（取代連點迴圈）。程式：`BattleDelays`、`DelayPrefs`、`DelaySettingsActivity`、`BattleScript` 套用、服務接線。
  - 描述：勝利後點「精靈恢復」太快，因遊戲本身延遲／切關延遲，可能沒真正按到、誤開非當前任務或選單，導致辨識失效。希望在總控制區加各段可調延遲(ms)，例如：精靈恢復→500ms→進入戰鬥→500ms→技能→500ms→撤退→…（每段可調）。#9：撤退時彈出的撤退鈕目前被點多次，想改為「延遲＋精準單點」。
  - 判斷：目前 `BattleScript`/`retreatAndRetry` 有多處硬編碼 `sleep(700/900…)` 與迴圈多次點擊，可抽成可設定值。
  - 做法：在 `SeerScript` 加一組延遲設定（如 `healDelay`/`enterDelay`/`skillDelay`/`retreatDelay`），設定 UI 可調；撤退流程改「延遲後精準單點」。（傾向延遲方案，非視覺方案。）
  - 關聯：`app/src/main/java/com/autoseer/core/SeerScript.kt`／`ScriptStore.kt`、`scripts/.../BattleScript.kt`、設定 UI（`activity_script_edit.xml`/`activity_main.xml`）。

- **FR-3｜編輯器滑動重排**（來源：建議_2 #1）🟡 中
  - 描述：目前改技能順序只能從尾端逐一刪除，很麻煩。希望像滑塊一樣可拖曳調整順序，提高靈活度。
  - 做法：技能序列改為可拖曳清單（每回合一個 chip，可拖動重排／插入／刪除任一步；`RecyclerView`+`ItemTouchHelper` 或等效）。
  - 關聯：`app/src/main/java/com/autoseer/ui/EditorActivity.kt`、`BattleEditorView.kt`。

- **FR-4｜單一腳本匯出**（來源：建議_2 #7）🟡 中（易做）
  - 描述：現在匯出是全部匯出；希望長按想匯出的腳本→顯示「複製此腳本」，只匯出一個。
  - 做法：`ScriptListActivity` 的 row 加 `setOnLongClickListener`→匯出單一 JSON（`ScriptStore` 加 `exportOne` 或用 `exportAllJson(listOf(one))`）。
  - 關聯：`app/src/main/java/com/autoseer/ui/ScriptListActivity.kt`、`ScriptStore.kt`。

- **FR-5｜周回目標分組（精靈因子等）**（來源：建議_2 #10）🟡 進行中（框架已落地，2026-09-22）
  - 描述：把「精靈因子」這種循序進關包成一個周回選項；未來會有多個不同任務，每個任務按鍵位置不同、需個別客製。流程：開 App→主頁（周回腳本、詳細設定…）→周回腳本→不同周回目標。
  - 判斷：現有多腳本系統已接近，缺「任務類型／座標組」概念（每個周回目標帶自己的座標與流程）。與 FR-6 相綁。
  - 已落地（Round 2）：`SeerScript.category`（seer_factor／daily）＋ `ScriptStore` 依分類過濾；主頁改 FGA 式（周回腳本／更多設定／更多選項／Debug）；周回腳本頁兩方塊（精靈因子→分類腳本清單；每日任務→佔位）。每日任務引擎尚未實作。
  - 關聯：`app/.../core/SeerScript.kt`、`ScriptStore.kt`、`ui/MainActivity.kt`、`MissionListActivity.kt`、`ScriptListActivity.kt`、`DailyTasksActivity.kt`。

- **FR-8｜精靈因子關卡（內）掃蕩腳本**（來源：標註 PPT `20260921` ＋ md `20260922`）🟡 進行中（引擎＋UI 已落地待實機，2026-09-22）
  - 描述：依標註 PPT 流程做精靈因子「關卡內」自動掃蕩：開啟挑戰→精靈恢復→（每日首次提示?→確認）→等恢復完成→進入戰鬥→等你的回合→依該關技能排序戰鬥→勝利→點繼續，循環至「達到每天操作上限」→確認→回大廳停止。
  - 實作（Round 2）：
    - 樣板自 PPT 1600×900 截圖裁出（12 張）：你的回合/進入戰鬥/勝利繼續（重裁）＋精靈恢復/恢復完成字樣/每日首次提示/達到上限/回大廳系列。
    - `SeerModules`（可復用視覺模塊，全螢幕 find→點在找到處）＋ `BattleTurnRunner`（每回合施放/換精靈/*N 至陣亡，打到勝利）＋ `SeerFactorScript`（外層流程組裝，關 index 用 `BattlePlan.stageIndexFor` 環繞）。
    - `AutoSeerService` 依分類選引擎：seer_factor→`SeerFactorScript`，其餘→`BattleScript`；延遲讀 `DelayPrefs`、執行選項讀全域 `RunPrefs`（循環輪數/預設技能格/戰前恢復）。
  - 缺件／待辦：①實機驗證整條流程與各樣板分數；②戰鬥失敗/撤退處理（目前 PLAN_EXHAUSTED 僅記錄）；③引擎單元測試（節奏/關 index/停止條件）尚未補。
  - 關聯：`scripts/.../SeerModules.kt`、`BattleTurnRunner.kt`、`SeerFactorScript.kt`、`SeerUi.kt`、`app/.../core/RunPrefs.kt`、`service/AutoSeerService.kt`、`app/src/main/assets/images/`。

- **FR-6｜座標優先、視覺輔助**（來源：建議_2 #11）🔵 中長期
  - 描述：多數關卡按鍵是固定位置，可由玩家截圖標註；視覺判斷加強在戰鬥或未來真正全自動。
  - 判斷：目前混用視覺（找「進入戰鬥」「等待挑戰」）與固定座標（`SeerLayout`）。可導入「座標優先、僅關鍵狀態（勝利／你的回合／已戰敗）用視覺」模式。
  - 關聯：`SeerLayout`、`BattleScript` 流程、樣板系統（`DeviceTemplates`）。

- **FR-7｜自動判斷目前在第幾關**（來源：建議_2 #3）🔵 長期
  - 描述：能根據畫面判斷現在在哪一關。
  - 判斷：為 FR-1／FR-6 的進階；需可靠樣板與穩定辨識。

---

## D. UX／文案

- **UX-1｜「預設技能格(1–5)」說明不清**（來源：建議_2 #4）🔴 文案待改（已口頭解釋）
  - 說明：指「**當某一關你沒有在計畫裡指定任何動作時，每回合要一直按的預設技能格**」（1＝圓形招牌技，2–5＝四個矩形技能）。也是 FR-1 循環 BUG 跑到未設定關卡時「一直按技能2」的來源。
  - 做法：改 `strings.xml` 的 `opt_default_slot` 標籤與說明，加一句白話與例子。
  - 關聯：`app/src/main/res/values/strings.xml`（`opt_default_slot`）。

---

## E. 觀察／診斷
- **OBS-1｜進戰後需按「存畫面」才會自動戰鬥＝截圖送舊影格**（來源：實機回饋 2026-09-18）🟡 已重修待實機（2026-09-22）
  - 重修（Round 1，commit 6974f75）：`ScreenCaptureManager` 改 `grabDeviceBitmap()`＝先 `acquireLatestImage().close()` 丟棄積壓的舊影格（等同「存畫面」多抽一張的效果），再輪詢取新影格；`takeScreenshot`/`captureColorBitmap` 皆走此路。CI 綠燈出 APK。**仍需使用者實機確認是否免按存畫面**；若仍失敗，改主動週期 poke VirtualDisplay 或改拉 `acquireNextImage`。
  - 以下為原始定位（保留）：
  - 現象：設定完腳本→精靈恢復→進入戰鬥都正常，進戰後不按技能；**按懸浮列「存畫面」後就開始施放**。
  - 決定性線索＋否證：「存畫面」只是多抓一張影格，不碰樣板/畫面/狀態，卻能解鎖 → 若是樣板品質問題，多抓影格不可能有效 → 分析用的是**舊影格**（你的回合沒進到被比對的影像）。
  - 根因（判斷）：截圖生產端背壓——腳本等待時抓太慢，`ImageReader` 緩衝填滿後 `acquireLatestImage` 一直回傳舊影格；手動「存畫面」多抽一張騰出緩衝才恢復。
  - 已嘗試（未解決）：`ScreenCaptureManager` 改 `setOnImageAvailableListener` 背景持續排空、`maxImages`→3（commit 6362fad）。**實機回報仍需按存畫面**，故此修**未奏效或未完整**。
  - 待重查方向：確認 listener 是否真的持續觸發／被排程；VirtualDisplay 在畫面「無變化」時是否根本不產出新影格（若是，需主動週期性 poke 或改用 `acquireNextImage` 持續拉）；或在每次 `refreshScreen` 前強制丟棄一張再取。
  - 關聯：`app/src/main/java/com/autoseer/capture/ScreenCaptureManager.kt`、`scripts/.../BattleScript.kt`（等待迴圈節奏）。

## 更新紀錄
- 2026-09-18：建立本檔，整理《AutoSeer 2026017 建議&回饋_2》共 11 條建議＋1 BUG。
- 2026-09-18：完成 FR-2 可調延遲（分段、全域）＋撤退精準單點。
- 2026-09-18：完成 BUG-1（重複授權不再疊浮窗）與 FR-1（精靈因子固定關卡循環／不再跑到關卡6）。
- 2026-09-18：OBS-1 定位進戰後不按技能＝截圖背壓送舊影格（非樣板）；首次修法（持續排空）實機仍需按存畫面，未奏效 → 依使用者要求延後處理、待重查。
- 2026-09-18：與使用者議定「以標註 PPT（1600×900、方框為獨立圖案、一頁一步）交付固定座標流程」，先做樣本、Claude 回讀疊圖確認，作為 FR-5/6/11 基礎（詳見 plan 檔）。
- 2026-09-22（Round 1）：OBS-1 重修（讀取前強制丟棄舊影格再取新）；自 PPT 生成精靈因子樣板 12 張；HTML mockup 定案（FGA 式主頁）。
- 2026-09-22（Round 2）：FR-8 精靈因子掃蕩引擎落地（SeerModules＋BattleTurnRunner＋SeerFactorScript＋服務依分類選引擎）；UI 落地（主頁 FGA 化、周回腳本分類、編輯精簡、更多設定/Debug/更多選項/每日任務佔位）。兩個 commit 各走 CI。缺件：實機驗證、戰鬥失敗處理、引擎單元測試。
