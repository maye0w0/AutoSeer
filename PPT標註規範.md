# AutoSeer 標註 PPT 製作規範

> 目的：讓你做流程 PPT 時有明確對照，讓我（Claude）解析時能**無歧義**分辨每個框的用途與流程順序，減少開工前的來回確認。
> 適用：精靈因子、每日任務、以及未來 backlog（自動跨關＋擷取精靈圖像＋選關 UI）等所有「教我操作流程」的 PPT。

---

## 1. 我能從 PPT 讀出什麼

用 Python 標準庫（`zipfile` + `xml.etree`）直接解析 `.pptx`，每頁我讀得到：

- **背景截圖**：每頁的遊戲畫面圖。
- **每個框（矩形/圖案）的位置與大小**：我換算成正規化 **1280×720** 座標（程式內部座標系）。
- **文字**，三個來源都讀：
  1. 框內打的字（矩形 shape 的 text）
  2. 框旁邊獨立的文字方塊
  3. 投影片下方的**講者備忘稿（notes）**
- **投影片順序** ＝ 操作流程的先後順序。

---

## 2. ⚠ 最關鍵的一條（不遵守座標會整體偏掉）

**遊戲截圖必須鋪滿整張投影片**：貼滿、不要縮小置中、不要留白邊、長寬比對齊投影片。

原因：我用「框在投影片上的比例」換算點擊座標。截圖若沒鋪滿、或被縮放置中，我算出來的位置會**整體偏移**，實機就會點錯地方。

其他約束：
- **每頁＝一個遊戲畫面狀態**（不要一頁塞多個畫面）。
- 同一畫面若要標多個位置，就在**同一頁**放多個框。

---

## 3. 框的用途：用顏色分類（提案，你可改）

光有框，我只知道「位置」，不知道那是「要偵測的圖示」「要點的固定位置」還是「要等的字樣」。請用框線顏色區分：

| 框色 | 用途 | 我會怎麼處理 |
|---|---|---|
| 🔴 紅 | 要裁成**圖像樣板**去偵測（matchTemplate） | 裁那塊圖存 assets，實機用比對找它、判斷是否存在 |
| 🟢 綠 | **固定座標點擊**位置（位置固定不需辨識） | 直接點框中心 |
| 🟡 黃 | **字樣判斷**（等某段文字出現當作閘門） | 裁該字樣做偵測，出現才往下走 |
| 🔵 藍 | 要**擷取/存檔**的區域（例：精靈圖像） | 存成圖檔，不點擊 |

> 紅 vs 黃差別：紅框是「要辨識存在/位置的圖示或按鈕」；黃框是「純文字提示，當流程閘門」。不確定就當紅框，並在備忘稿說明。

---

## 4. 文字附註怎麼寫

三種方式我都讀得到，依情境選：

1. **講者備忘稿（最推薦，寫長說明）**：投影片下方那塊，不佔畫面、不遮圖，適合寫每步的動作與分支。
2. **獨立文字方塊**：放在框旁邊、用線/箭頭指向框，適合短標籤。
3. **框內打字**：點框直接輸入，字嵌在框內——我讀得到，但字會**蓋在遊戲截圖上、遮住框住的按鈕**，只建議放極短標籤（如「精靈恢復」）。

### 備忘稿格式（推薦：分欄式，穩、清楚）

沿用分欄寫法，每個框寫這四項——這正是我需要的四個資訊，分欄比擠成一行更不易誤讀：

```
類型：等待出現(視覺) / 固定點擊 / 字樣判斷 / 擷取存檔
目標是什麼：指哪個框（用顏色或左上角座標指名）、框住什麼
為什麼/下一步：要做什麼動作 → 之後去哪 → 有無分支
```

範例：
```
類型：等待出現(視覺)
目標是什麼：等待紅框位置的「因子關卡」字樣後點擊
為什麼/下一步：等待紅框「因子關卡」字樣出現 → 點擊 → 進入「因子關卡頁面」
```

**多框/重疊頁**：在「目標」欄用顏色或左上角座標指名是哪個框。
**有分支的頁**：在「下一步」欄寫清楚「什麼條件走哪」（例：勝利→下一關頁；失敗→繼續挑戰）。

### 一行式（簡易備選，僅單框、無分支的頁想省事時用）
```
[步驟N][顏色/類型] 名稱：出現後要做什麼、點它會去哪
```
例：`[步驟5][🟡黃] 恢復完成字樣：出現「已全部恢復/已滿」才算完成，再進入戰鬥`

> 想寫詳細一律用分欄式，更保險；一行式只是單框簡單頁的縮寫。

---

## 4.5 重疊框可以嗎？

可以，但要分「能不能偵測」和「能不能判斷用途」兩件事：

- **幾何（能偵測）**：每個框都是獨立的 shape、各帶自己的座標。**不管有沒有重疊、誰蓋誰、疊放順序如何，我都逐一讀到每個框的完整座標。**
- **語意（有限制）**：重疊時我知道「有兩個框」，但光靠幾何**不知道哪個框對應哪個用途/畫面元素**。若兩框同色又都沒備忘稿說明，我可能分不出誰是誰，或誤當同一個判斷。且底層框被蓋住時，**你自己也會看不清它框住什麼**、容易標錯。
- **群組（Group）**：能處理，但群組後座標要套用群組位移換算，較易出錯——**建議重疊框不要群組**。

**做法**：
- 重疊的每個框**用不同顏色**（用途不同）。
- 每框在**備忘稿各寫一行**，用「左上角座標」或「顏色」指名（例：`[🔴紅框] 精靈頭像／[🟡黃框] 上場提示字`）。
- 底層框若被蓋住，說明用**引導線拉到空白處**，別把字疊上去。

只要顏色＋備忘稿能一對一指名，重疊框就能正確落地；無法指名時我會回頭問你，不亂猜。

## 5. 流程分支怎麼標

- **順序**：靠投影片先後。分支（勝/敗、有/無提示）就用不同頁分別畫，並在備忘稿寫清楚「什麼條件走這頁、之後回到第幾頁」。
- **箭頭/連接線**：我讀得到幾何位置，但**語意不保證**，別只靠箭頭表達分支——請在備忘稿用文字補一句。

---

## 6. 未來 backlog 專用：自動跨關＋擷取精靈圖像＋選關 UI

要開工這項，PPT 需涵蓋以下頁（每頁截圖鋪滿、依上面慣例標框）：

1. **精靈因子選關地圖**：框出各關卡節點位置、目前所在，並說明**怎麼前往下一關**（點哪個節點／往哪個方向滑／滑多少）。
2. **節點 ↔ 關卡對應**：哪個節點是第幾關、進去後長什麼樣。
3. **精靈圖像在哪**（🔵藍框）：精靈頭像/立繪出現在哪個區域，以及你想要的**存檔命名規則**（例：關卡名_精靈名.png）。
4. **節點狀態差異**：可挑戰／已刷完／鎖定 分別長怎樣，讓我判斷該不該進。
5. **邊界情況**：地圖捲動到底、沒有下一關、網路/載入中等畫面。

---

## 7. 快速檢查清單（做完 PPT 前對一遍）

- [ ] 每頁截圖鋪滿整張投影片、沒縮小置中、沒白邊
- [ ] 每頁只有一個遊戲畫面
- [ ] 每個框有顏色（用途明確）
- [ ] 每個框在備忘稿有一行說明（含步驟號與分支）
- [ ] 分支頁有寫「什麼條件走這頁、之後回到哪頁」
- [ ] （擷圖類）藍框區域＋命名規則已寫

> 依此規範產出的 PPT，我能把框的用途、座標與流程順序無歧義解析並落地。若有任何一頁不確定，寧可在備忘稿多寫一句，也別讓我猜。
